---
title: "dada_manage"
output: html_document
date: "2023-04-19"
---

#challage 1.1
```{r}
mass <- 47.5            # mass?
mass
age  <- 122             # age?
age
mass <- mass * 2.0      # mass?
mass
age  <- age - 20        # age?
age
mass_index <- mass/age  # mass_index?
mass_index
```


```{r}
weight_kg <-  sqrt(10)
weight_kg

round(3.14, digits = 2)

weight_g <- c(50,60,65,82)
weight_g

animals <- c('mouse', 'rat', 'dog')
class(animals)
class(weight_g)
```
#challange 1.2
```{r}
  num_char <- c(1, 2, 3, "a")
  num_logical <- c(1, 2, 3, TRUE)
  char_logical <- c("a", "b", "c", TRUE)
  tricky <- c(1, 2, 3, "4")
  
  class(num_char)
  class(num_logical)
  class(char_logical)
  class(tricky)
  
```

```{r}
animals[-c(1,3)]

weight_g <- c(21,34,39,54,59)
weight_g > 50
weight_g[weight_g>50 & weight_g < 30]

height <- c(2,3,4,NA,6)
mean(height, na.rm = TRUE)

class<-(height)

```

# version control git 
```{r}

```

